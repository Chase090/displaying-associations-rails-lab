<h1>List of Artist</h1>
    
    <ul>
        <% @artists.each do |a|%>
        
        <li> <%= link_to a.name, artist_path(a) %> </li>
    (<%= a.song_count %> <%= "song".pluralize %>)
        <%end%>
    </ul>
